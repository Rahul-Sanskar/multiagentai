# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.1

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0
structlog==24.1.0
python-dateutil==2.9.0

# NLP — topic extraction, tone detection
nltk==3.8.1
scikit-learn==1.3.2
numpy==1.26.4

# RAG — vector search
# faiss-cpu 1.7.4 has pre-built wheels for Python 3.11 on Linux (Render)
faiss-cpu==1.7.4

# Embeddings — sentence-transformers pulls torch; pin to avoid 2GB+ download
# torch CPU-only is ~700MB which fits Render free tier build limits
sentence-transformers==2.7.0
